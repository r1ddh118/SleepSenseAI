# Legacy Hardware

Archived hardware assets belong here so the primary API/Celery/Spark workflow
does not depend on ESP32, MQTT, or device-specific libraries.

No `withMPU/withMPU.ino` file was present in the repository during this
migration step. If that asset is recovered later, move it to
`legacy/hardware/withMPU.ino`.
